"use client";

import { ArticlesManager } from "@/components/admin/ArticlesManager";

export default function ArticlesPage() {
  return (
    <ArticlesManager 
      title="Articles Manager"
      description="Manage all dynamic wisdom posts, reflections, and articles from a single dashboard."
    />
  );
}
